ServiceMaps:
  serviceName: Health Notification Service
  version: 1.0.0
  mappings:
    # ── INDEX: Save new scheduled notification to Elasticsearch ──
    - topic: kwara-save-scheduled-notification-topic
      configKey: INDEX
      indexes:
        - name: kwara-scheduled-notification-v1
          type: general
          id: $.id
          isBulk: true
          jsonPath: $.*
          timeStampField: $.auditDetails.createdTime

    # ── INDEX: Update scheduled notification in Elasticsearch ──
    - topic: kwara-update-scheduled-notification-topic
      configKey: INDEX
      indexes:
        - name: kwara-scheduled-notification-v1
          type: general
          id: $.id
          isBulk: true
          jsonPath: $.*
          timeStampField: $.auditDetails.lastModifiedTime

    # ── REINDEX: Re-index from Elasticsearch ──
    - topic: kwara-scheduled-notification-v1
      configKey: REINDEX
      indexes:
        - name: kwara-scheduled-notification-v1
          type: general
          jsonPath: $.hits
          timeStampField: $.lastModifiedTime